# Public env vars consumed by the frontend (Next.js).
# Copy to `.env.local` and then RESTART `npm run dev`.

# Which Sui network to use: localnet | devnet | testnet | mainnet
NEXT_PUBLIC_SUI_NETWORK=testnet

# Optional: override RPC url for the selected network (otherwise uses Mysten defaults)
# NEXT_PUBLIC_SUI_RPC_URL=

# Optional: backend/indexer (if set, UI reads events from backend instead of directly from chain)
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Required: deployed Move package + shared objects (from `init_state` Initialized event)
NEXT_PUBLIC_SUI_PACKAGE_ID=
NEXT_PUBLIC_COURSE_CATALOG_ID=
NEXT_PUBLIC_PROPOSAL_REGISTRY_ID=

# Optional: show hardcoded mock courses in the UI (useful before deploying)
# NEXT_PUBLIC_USE_MOCK_DATA=1


